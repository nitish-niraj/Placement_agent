# 10 — Project Handoff (state + the one open problem)

| | |
|---|---|
| Date | 2026-09-09 |
| Purpose | Complete context to continue in a fresh session |
| Repo | `E:\agent\placement-intelligence` · GitHub: `nitish-niraj/Placement_agent` (public) · CI green on every push |
| Stack | Python 3.11 venv `.venv/Scripts/python` (Git Bash), Docker Compose: postgres(pgvector)/redis/minio/api/worker/evolution-api v2.3.7, all `restart: unless-stopped`, daily 09:30 DB backup (Task Scheduler "PIA daily backup") |
| **Update 2026-09-12 (final)** | **KYC listener COMPLETE and LIVE-VERIFIED (runs 12–13 on a real meeting):** ✅ joins as the authenticated LPU account (~20 s via the in-page sign-in dialog), ✅ mic+camera verified OFF before/at join (`media_off_confirmed`), ✅ captions via More → Language and speech → Show live captions with pane verification + transcript captured, ✅ join-proof screenshot sent to Telegram, ✅ form link relayed instantly (+ presenter-name detection from caption self-intros) then auto-leave. Run 12 transcript proves real speech lands. |
| **Update 2026-09-13** | **P12 completed + Ask PIA on Telegram.** Two live bugs found and fixed in `search/answer.py`: (1) the question was never sent to the LLM (model honestly replied "no question was provided") — prompt now leads with `QUESTION: …`; (2) Ask had no degradation ladder — it now rides **DEC-010** (NIM structured → OpenRouter → Groq → deterministic) via the generalized `pia_worker/ai/fallback.py` (listener's live-verified copies untouched); results carry a `source` field (dashboard shows "answered via X"). Deterministic run is question-aware: "latest eligible company" answers newest `detected_at` from stored records. NEW: `pia_worker/telegram_ask.py` — long-polling bot (compose service `telegram-ask`, no inbound port, owner-gated by `TELEGRAM_CHAT_ID`, Redis-acked offset, `/start` help) answering the same ladder in Telegram. `ask_retrieve_k` wired into retrieval. Gates: ruff ✓ mypy ✓ 311 tests ✓ (unit tests stub the ladder rungs — integration collection loads real provider keys into the env). |
| **Update 2026-09-13 (P13)** | **P13 delivered: form/KYC detection + approval workflow.** DEC-008 Amendment 2026-09-13 (owner): form/document SUBMISSION automation in scope behind the approval workflow — attendance at post-shortlist evaluative sessions stays always manual. F-029: shared `extract_form_links` in `events/rules.py` (listener now imports the same pattern); `resolve_event_type` turns a bare form link into a FORM event; FORM priority = HIGH immediate in `notify/decide.py`; a dated form event gets T-24/6/1h reminders via the existing sweep. F-030: `jobs/propose_actions.py` creates a `form_draft` action (PROPOSED→WAITING_APPROVAL asserted, audited, idempotent per URL, payload = references only); new API `routes/actions.py` (GET /actions with read-time pre-fill decrypted via SEC-002 path, POST approve/reject guarded by `assert_valid_transition`, 409 on illegal hops, everything audited; NO execution path — APPROVED terminal until P14 executor ADR); dashboard **Approvals** screen (`/approvals`) with per-draft Approve/Reject. No migration, no new service. Gates: ruff ✓ mypy ✓ 340 tests ✓. Next: P14.1 submit executor — needs one real KYC form URL to build against. **Listener bridge (same day):** `_relay_form_link` now also calls `propose_meeting_form_action(link, presenters)` — a form link dropped in the Teams meeting chat becomes an approval draft carrying the caption-detected teacher/presenter names (the feedback form's key field); Telegram relay unchanged, DB write best-effort (never breaks the session); host runs assemble DATABASE_URL from POSTGRES_* (localhost:5432). Live-verified host→DB→API→approve cycle. **P14.1 built (ADR-012):** `automation/` package — smart field mapper (deterministic hints → LLM may only choose catalog keys → skip+report; new columns never fail the run), Playwright Google-Forms driver (dropdown fuzzy match for the teacher name, multi-page refused to manual), and the submit executor (APPROVED→EXECUTING→SUCCEEDED/FAILED, audited; FORM_AUTOMATION_ENABLED + FORM_SUBMIT_DRY_RUN=true default). Approve enqueues the job when enabled; worker image ships headless chromium. 366 tests ✓. First real form tunes selectors — dry-run makes that safe. **ADR-011 Stage 1 built (same day):** `pia_worker/agent/` — bounded ReAct (schema-validated AgentDecision steps, AGENT_MAX_STEPS=6) over read-only tools; degradation agent→P12 ladder; traces in `agent_traces` (migration 20260913_0001, applied live); `POST /api/v1/agent/ask`; dashboard Ask 🧠 Deep toggle with reasoning trace. 380 tests ✓. **Stage 2 built (same day):** `agent/reviewer.py` — daily proactive reviewer 30 min post-digest (REVIEWER_ENABLED, worker thread): read tools + propose_action (allowlisted types, max 3/run, dedup, evidence required) filing proposals on the Approvals screen + Telegram summary; silent skip on LLM failure; `GET /agent/traces` + Audit-screen traces card. 390 tests ✓. **Stage 3 built (same day, ADR-013):** `executors/proposal_executors.py` — approved proposals execute: deadline_nudge/kyc_reminder/follow_up -> Telegram reminder card; verify_field -> event field correction (jsonb_set + before/after audit, field allowlist); data_quality -> idempotency clear + deterministic re-parse re-enqueue. STAGE3_EXECUTORS_ENABLED switch; approve enqueues per type; Approvals screen states what approving will do. 404 tests ✓. **Stage 4 built dormant (same day, ADR-014):** `agent/reactive.py` — one second-opinion judgment for GENERAL/UNKNOWN + MEDIUM/LOW messages (escalate/digest/ignore; rule veto, confidence 0.7, cap 3/day, audited); `STAGE4_REACTIVE_ENABLED=false` — enable only if a misclassified alert is observed. 411 tests ✓. |

| **Update 2026-09-22 (hygiene)** | **Repo cleanup, no behavior change.** `E:\agent` root probes/logs (`probe*`, `listen_run*`, `login_save_run*`, `autologin*`) archived to `.attic-2026-09-12/` (gitignored); `.gitignore` fixed (`.env-backup*` dash pattern, attic, removed dupes). `teams/_shared.py` holds `_is_login_url` / `_resolve_link` / `_direct_meeting_url` / `EDGE_UA` verbatim — `listener.py` re-exports them. `login_save --auto --meeting URL` replaces archived `autologin.py` (env creds, LPU-address verify, never saves on failure). New `scripts/`: `run-listener.ps1`, `refresh-session.ps1`, `clean-transcripts.ps1` (dry-run retention, 90 files). Gates: ruff ✓ mypy ✓ 486 unit ✓ 4 integration ✓ (needs `.env` sourced) `/health` ok. Open: transcript `-Apply` purge (operator-run). |
| **Update 2026-09-22 (DEC-011)** | **Application-aware notifications (company mentioned != applied).** New `application_states` table (migration `20260922_0001`, per company+role: UNKNOWN/ELIGIBLE_NOT_APPLIED/APPLIED/NOT_APPLIED/NOT_SURE/NOT_INTERESTED, user-driven, audited). Deterministic classifier `notify/classify.py` (pre/application-related/post-action/generic + penalty detection). Gates: `notify_event` (suppress not-applied/not-in-list, ask-nudge with inline buttons for undecided, priority boost for APPLIED+penalty), `propose_form_action` (skip drafts for not-applied/not-in-list), `deadline_escalations` (same), strict CSV/image list gate via `message_list_gate` (Mars.AI-class spam ends). Eligibility alert now asks "Have you applied?" with buttons; `telegram_ask.py` persists `app:<id>:<STATUS>` callbacks; dashboard `GET/POST /api/v1/applications`; `get_applications` agent tool + reviewer prompt guidance (context-only, no hard gate). Gates: ruff ✓ mypy ✓ 542 unit ✓ 4 integration ✓, API rebuilt + live-verified (answer/list round-trip, test row cleaned). |
| **Update 2026-09-23 (DEC-012)** | **Notification pipeline deep fix.** Context-first `ResolvedContext` (subject/topic/verification/deadline/recommendation/evidence) drives student-first templates (ACTION/INFO/OPPORTUNITY/stage/EXPIRED/VERIFY) — no more "General — REGISTRATION". Topics in pure Python + system-test filter; subject chain (company → filename → topic token → 📋 fallback, never invented). Attachment verification strict states; downloads fail fast with codes, transport retries backoff, Evolution `getBase64FromMediaMessage` refetch proven by docs spike, `retry_attachment`/`reap_stuck_attachments` added; retention purges terminal states only. Watchdog internals → logs + optional admin chat, student gets one plain heartbeat line max. Deadline truth (past-due never TODAY; moved deadlines re-arm). Categorized digest with per-day idempotency anchor. Reminder-shaped actions merge; deltas render as updates. Flags wired (all default-on except debug). Gates: ruff ✓ mypy ✓ 634 unit ✓ 9 integration ✓. Integration caught 2 real SQL bugs pre-merge (event_updates.detected_at, attachments alias). |
| **Update 2026-09-23 (backlog + strangler)** | **Backlog sweep:** reaped 17 stuck DOWNLOADING attachments (oldest Sept 5, incl. real lists) to coded FAILED; `resurface_stuck_notifications()` triaged 24 QUEUED/PENDING_DELIVERY rows (6 live rebuilt + resent, 18 stale buried with reasons, zero verbatim resends); weekly `review_digest` (new-items-only, 30d age-out, per-week anchor, delivery-failure releases anchor) sent 97 items on first run; retention purges terminal states only. **Teams strangler:** `listener.py` 1341→568 lines via `summary.py` → `chat.py` → `captions.py` (+`pane_health`) → `auth.py` (predicates, unified `drive_login_page`, cookie-expiry `session_status` probe) → `prejoin.py`; listener keeps thin re-exports. New behavior: pre-launch session probe + plain-language expiry/guest-join alerts; dead caption pane pages once (join + mid-watch). Settings compose `DATABASE_URL` from `POSTGRES_*` (worker+API). Gates: ruff ✓ mypy ✓ 678 unit ✓ 9 integration ✓ (no DATABASE_URL needed). |
| **Update 2026-09-23 (items 5+6)** | **Secrets rotated + ops blind spots closed.** Deleted `E:\agent\.env-backup-20260921`; audit: `.env` never committed, no token patterns in tracked source. Rotated `POSTGRES_PASSWORD` (ALTER USER + compose recreate, health green) and `DASHBOARD_TOKEN` (API verified); university/Microsoft password + bot token need human rotation (BotFather/LPU portal). Watchdog now monitors Evolution instance state (closed → plain-language page; verified live: `open`), telegram-ask heartbeat with stall notes, and a 24h LLM-failure digest from `ai_call_logs` (dead providers page admin-only). Isolated `pia_test_*` scratch DBs per integration test (migrated, seeded, dropped, zero leaks) — `test_notification_flow.py` runs fully isolated. Gates: ruff ✓ mypy ✓ 696 unit ✓ 9 integration ✓. |
| **Update 2026-09-23 (items 3+4)** | **Company hygiene + classifier feedback.** `resolve_company` no longer mints rows by default (`create=False`); drive-code events, eligibility lists, and fixtures pass `create=True` explicitly; raw mentions link nothing unknown. `gc_orphan_companies` (daily maintenance) reaped 4 orphans live (2 younger kept under grace). Lifecycle funnel past ELIGIBLE retired by decision (per-role truth is `application_states`; enum/column stay frozen). Feedback loop: `classification_feedback` table (migration `20260923_0001`) + `POST/GET /api/v1/feedback/classification` (predicted-vs-correct, audited, history untouched; corpus promotion stays manual), live-verified with cleanup. Gates: ruff ✓ mypy ✓. |

## 1. Done & verified (P0–P12 + extras)

- **Pipeline**: WhatsApp ingestion (idempotent, 4-layer dedup: exact-7d-rule/visual dHash/near token-Jaccard ≥0.72 **company-gated**/semantic) → hybrid classification (rules>LLM) → document intelligence (XLSX/PDF/image → 2,471 real rows) → **eligibility engine** (identifier-dominance ladder; Nitish/12515641 found in 5/6 real lists; Singh/12528230 correctly NOT_FOUND) → **company memory** (resolver, aliases, watch-state auto-activation, lifecycle machine) → **events/deadlines** (deterministic 12-type rules + IST date parser + drive fields: designation/salary_package/job_location/eligibility_note from `*Designation :-*` template) → **notifications** (Telegram §11.1 template, CRITICAL/HIGH immediate, digest 20:30, escalations T-24/6/1h, delivery history, dedup anchors) → **dashboard** (React+TS 10 screens, served by API container at :8000, bearer `DASHBOARD_TOKEN`) → **Ask PIA** (P12 hybrid: pgvector `nemotron-3-embed-1b` 2048d + keyword + structured facts; schema-validated LLM answer with citation filtering; deterministic evidence fallback)
- **KYC Type-1 listener** (`apps/worker/src/pia_worker/teams/`) — DEC-008 amended: informational company-arrival sessions may be auto-attended with the owner's own login; post-selection KYC is ALWAYS manual.
- Gates: ruff ✓ mypy ✓ 292 tests ✓; gitleaks in CI; `.env`/`teams_session.json`/`transcripts/`/`backups/` gitignored.

## 2. What the listener already achieves (proven live)

1. `login_save` (run on host, headed): programmatic Microsoft login → `infrastructure/teams_session.json` (550 KB, validated authenticated) ✅
2. `listen(url)`: resolves tinyurl/shorteners → **decodes the launcher to the direct meeting URL** (avoids the `ms-teams:` protocol dialog) → warms the app shell at `https://teams.cloud.microsoft/` (15 s, **realistic Edge UA required** — default UA makes the SPA hang) → opens the meeting → guest name entry ("Nitish Kumar") → **JOINS** ✅ (several live tests)
3. Post-join: opens chat, enables+verifies captions, watches chat for Google-Forms links → **instant Telegram relay + join-proof photo**, leaves ~30 s after the link (final rung; see §4 item 6) ✅
4. After leaving: `MeetingSummary` via a **4-rung fallback ladder (DEC-010)** — NIM structured (`nvidia/nemotron-3-super-120b-a12b`) → OpenRouter (`inclusionai/ling-3.0-flash-vl:free`) → Groq (`openai/gpt-oss-120b`, 1K req/200K tok/day free) → honest raw transcript. Each rung tags its source; any timeout/402/429/invalid fails over. Ladder order locked by `tests/unit/test_listener_summary.py`. Host runs need `.env` sourced for all three keys ✅
5. Captions: transcript capture **works when captions are ON** (real speech captured: "Hello this is Mike testing 1-2 three"); the AUTO-ENABLE is the open issue below.

## 3. THE open problem (exactly two sub-issues) — ✅ fixed 2026-09-12, see §4

**A. Authenticated join — "Sign in" on the pre-join screen isn't reliably driven.**
Without it the participant shows as **"Nitish Kumar (Unverified)"** (guest identity) instead of the verified account (`TEAMS_EMAIL` in `.env`). Owner wants verified identity.
Observed behavior (from ~8 experiments):
- The pre-join has exactly **one** "Sign in" text control (a 52×17 px link near the bottom). Clicking it does NOT reliably produce a new popup (`context.expect_page` timed out at 20 s twice), and same-tab navigation either stalls 30 s+ at the same URL or — in ONE success — completed: the later join_failed diagnostic captured the page at `teams.microsoft.com/v2/` showing **the authenticated identity + "Join now"** (pre-join had become signed-in, join simply not clicked in time).
- Conclusion: the sign-in sometimes opens a popup window (must register `context.on("page")` BEFORE clicking and drive email→password→"Stay signed in: Yes" inside it — creds now in `TEAMS_EMAIL`/`TEAMS_PASSWORD` in `.env`), and sometimes needs longer patience. Current code polls all pages but times out too early and re-fills the name field after the sign-in hop (the authenticated pre-join has NO name field — that's normal).

**B. Media-off not guaranteed.** The join loop's `get_by_label("camera"/"mic")` toggles miss on this UI (user screenshot proved camera ON at join; in-meeting `_mute_if_live` fallback also missed the meeting-bar buttons). Need: pre-join — click the camera/mic icon **buttons** (aria-labels observed in the menu dump: "Turn camera off"/"Mute mic" appear only when ON; the pre-join buttons have labels like "Camera"/"Microphone" with state); in-meeting — the meeting-bar buttons expose aria-label "Mute"/"Unmute" (check `data-tid` or `aria-pressed` states).

## 4. What was applied (2026-09-12) — then superseded/validated by live runs 5–13 below

**A. Authenticated join** — `listen()` join loop rewritten:
- `context.on("page", …)` registered BEFORE the pre-join 'Sign in' click (popups logged from birth, no `expect_page` race).
- Any page on a login domain (`_is_login_url`: login.microsoftonline / login.live / account.live / microsoftonline) is driven every cycle: `input[type=email]`/`loginfmt` → `#idSIButton9`, `input[type=password]` → `#idSIButton9`, then Yes/Next/Accept. This covers BOTH the popup and the same-tab navigation variants.
- Guest name is filled ONCE per page and re-filling is suppressed for 60 s after the 'Sign in' click (`signin_pending_until`) — the authenticated pre-join has no name field, and "Join now" appearing there is the success signal. A missing "Join now" while a login page exists is just "wait" (the loop polls on).

**B. Media off** — state-driven, no more blind toggling:
- ROOT CAUSE of camera-ON-at-join found: the old loop clicked `get_by_label("camera")` every 2 s — a blind re-toggle that switched an already-off device back ON.
- New `_turn_media_off_prejoin`: one decisive pass per page; reads every `button[aria-label]`, classifies LIVE (`Turn camera off`/`Mute mic`/`Mic on`/`With camera on`, `aria-pressed=true`) vs OFF (`Unmute`/`Turn camera on`/`Camera off`) via anchored regexes (`_LIVE_RE`/`_DEAD_RE` — live wins: "Turn camera off" contains "camera off") and clicks ONLY the live ones.
- `_mute_if_live` (post-join safety net) rebuilt on the same classifier — exact aria-label logic, never role+name substring matching (which also hits "Unmute mic").
- Diagnostics kept: `join_failed_*` dumps + `_dump_controls` (the `controls_*` menu dumps supplied the exact labels used above).

**What the live runs (5–13, same day) actually proved — and what changed on top of the above:**
1. **The real auth surface is an IN-PAGE dialog** (`role=dialog`, "Enter your email or phone number" + Next) — no popup, no iframe, no login URL ever appears. The earlier "drive login.microsoftonline pages" detectors could never see it (probe5 evidence). `_drive_signin_dialog` now handles each case: email → pick-account → password → "Stay signed in: Yes" → MFA (logs, waits for the phone) → credentials-error (loud). Filling the email makes Teams SSO through the org session in ~20 s and lands on the authenticated app-shell pre-join (`teams.microsoft.com/v2/`, no name field) → `joined_as identity=authenticated`. The §3A popup/same-tab conclusion was wrong; the old docs' "Sign in is inert" impression came from the flyout never being filled.
2. **The 'Privacy and cookies' flyout that blocked joins IS that login dialog** (footer text). The old consent-dismiss code was CLOSING it — self-sabotage. `_dismiss_consent` now never touches it while a dialog input is present.
3. **Run 11 crash root cause**: after the auth redirect, Teams CLOSES the tab that hosted the pre-join; any later call on it threw `TargetClosedError` and killed the run (this caused the "sometimes it forgets media/captions" reports). Post-join now `_resolve_live_page` (finds the tab with 'Leave'/lobby), all waits are `_safe_wait`, and a lobby screen is watched + surfaced (`listener_in_lobby`) instead of silently "working".
4. **Media-off guarantee**: verified re-check loop post-join until the classifier finds NO live-state control (`media_off_confirmed`). Classifier precedence matters: 'Turn camera ON' = off-state (run 9 bug: matched as live and turned the camera on).
5. **Captions (owner-supplied path)**: More → Language and speech → **Show live captions**. Traps found live: `get_by_role(name="More", exact=False)` hits the app-shell "Settings and more" gear (use `^More$`); flyout items have no exact-text node (click via accessible name/role); 'Record and transcribe' starts persistent cloud transcription — NEVER click it (transient captions only); and the toggle needs click-ONCE-then-verify (a second click switches it back off). Run 12 captured real speech; run 13 verified in 1 s (caption state remembered).
6. **Owner asks implemented**: join-proof screenshot → Telegram `sendPhoto`; form relay now includes 👤 teacher/presenter names detected from caption self-introductions ("this is X", "I'm X"); leave 30 s after the form link (was 2 min).

**Verification commands** (unchanged): `set -a; . ./infrastructure/.env; set +a` then
`.venv/Scripts/python -m pia_worker.teams.listener "<url>" --minutes 4`.
If the session stops authenticating, re-run `.venv/Scripts/python -m pia_worker.teams.login_save "<meeting-url>"` — it now also trains the consumer dialog and only saves when the LPU address is on screen.

## 5. Commands & locations

```bash
# stack
cd E:/agent/placement-intelligence/infrastructure && docker compose up -d
# listener CLI (host, .env sourced for the LLM key):
set -a; . ./infrastructure/.env; set +a
.venv/Scripts/python -m pia_worker.teams.listener "<meeting-url>" --minutes 4
# re-save session if expired:
.venv/Scripts/python -m pia_worker.teams.login_save
# gates
.venv/Scripts/python -m ruff check . && .venv/Scripts/python -m mypy apps/api/src apps/worker/src packages/shared/src && .venv/Scripts/python -m pytest -q
```
Files: `apps/worker/src/pia_worker/teams/{listener.py,login_save.py,__init__.py (is_teams_url)}`, tests `tests/unit/test_ask.py` etc., diagnostics in `transcripts/`, env keys `TEAMS_EMAIL/TEAMS_PASSWORD/EMBEDDING_MODEL/NOTIFY_*` and the DEC-010 summary providers `NVIDIA_API_KEY`, `OPENROUTER_API_KEY/_MODEL`, `GROQ_API_KEY/_MODEL` (all free tier, all gitignored via `infrastructure/.env`). Docs: 00–09 + this handoff; thresholds: docs/07, docs/08.
